apply plugin: "groovy"
apply plugin: "maven"

group = "org.scaldingspoon.gradle"
version = "1.0.0-SNAPSHOT"

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

task wrapper(type: Wrapper) {
    gradleVersion = "1.8"
}
